# Modified commands.
alias ls='/usr/bin/lsd'
alias diff='diff --color=auto'
alias mkdir='mkdir -p'

# New commands.
alias f='sudo updatedb && sudo locate -i'
alias i='ipython3'
alias open='xdg-open'
alias p='paru'
alias t='tmux'

# Emacs.
alias e='TERM=tmux-direct emacsclient -t'
alias "e."='e . ..'

# Git.
alias git='hub'
alias gpush="git push -u origin \`git rev-parse --abbrev-ref HEAD\`"
alias 'g..'='cd \`git root\`'
